spring:
  datasource:
    url: "jdbc:mysql://localhost:3355/auto?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false"
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
